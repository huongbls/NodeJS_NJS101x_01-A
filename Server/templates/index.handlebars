<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Message</title>
  </head>
  <body>
    <h4>Xin chào {{fullname}}</h4>
    <p>Mã khách hàng: {{userId}}</p>
    <p>Điện thoại: {{phone}}</p>
    <p>Địa chỉ: {{address}}</p>
    <p>Chi tiết đơn hàng</p>
    <table class="table" style="border: 1px solid black;">
      <thead class="thead-light" style="border: 1px solid black;">
        <tr style="border: 1px solid black;">
          <th>Tên sản phẩm</th>
          <th>Hình ảnh</th>
          <th>Giá</th>
          <th>Số lượng</th>
          <th>Thành tiền</th>
        </tr>
      </thead>
      <tbody style="border: 1px solid black;">
        {{#each cartItem}}
          <tr>
            <td>{{nameProduct}}</td>
            <td style="text-align:center"><img
                width="100"
                height="100"
                src="{{img}}"
                alt="..."
              /></td>
            <td style="text-align:center">{{priceProduct}}</td>
            <td style="text-align:center">{{count}}</td>
            <td style="text-align:center">{{multiple priceProduct count}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
    <p>Tổng thanh toán: {{totalPrice}} VND</p>
    <p>Cảm ơn bạn.</p>
  </body>
</html>